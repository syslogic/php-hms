{
  "name": "syslogic/php-hms",
  "description": "Unofficial PHP SDK for Huawei REST API",
  "minimum-stability": "stable",
  "type": "library",
  "license": "MIT",
  "authors": [{
      "name": "Martin Zeitler",
      "homepage": "https://github.com/syslogic"
  }],
  "support": {
    "docs": "https://github.com/syslogic/php-hms/README.md",
    "issues": "https://github.com/syslogic/php-hms/issues",
    "source": "https://github.com/syslogic/php-hms"
  },
  "funding": [{
    "type": "github",
    "url": "https://github.com/sponsors/syslogic"
  }],
  "scripts": {
    "test": "export XDEBUG_MODE=debug && vendor/bin/phpunit --bootstrap vendor/autoload.php --configuration phpunit.xml --testsuite main",
    "coverage": "export XDEBUG_MODE=coverage && vendor/bin/phpunit --bootstrap vendor/autoload.php --configuration coverage.xml --testsuite main",
    "post-update-cmd": "ls -la"
  },
  "scripts-descriptions": {
    "test": "Run phpunit tests",
    "coverage": "Run phpunit tests with coverage",
    "post-update-cmd": "Occurs after the update command has been executed, or after the install command has been executed without a lock file present."
},
  "require": {
    "php": ">=8.0",
    "ext-json": "*",
    "ext-curl": "*",
    "phpseclib/phpseclib": "^3.0.12"
  },
  "require-dev": {
    "phpunit/phpunit": "^9.5.11"
  },
  "autoload": {
    "psr-4": {
      "HMS\\": "src"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Tests\\": "tests"
    }
  }
}
